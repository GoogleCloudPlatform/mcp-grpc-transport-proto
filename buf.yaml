version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Keep these for now - structural choices
    - DIRECTORY_SAME_PACKAGE
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
